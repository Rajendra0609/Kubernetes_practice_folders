apiVersion: v1
kind: LimitRange
metadata:
  name: label-lab-limits
  namespace: label-lab
spec:
  limits:
    - type: Container
      defaultRequest:
        cpu: 100m
        memory: 128Mi
      default:
        cpu: 500m
        memory: 512Mi
